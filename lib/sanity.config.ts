/**
 * Shared Sanity configuration
 * Used by both the Sanity client and Studio
 */
export const sanityConfig = {
  projectId: 'uusrx3n3',
  dataset: 'blogs',
  apiVersion: '2025-11-29',
  useCdn: true,
} as const
